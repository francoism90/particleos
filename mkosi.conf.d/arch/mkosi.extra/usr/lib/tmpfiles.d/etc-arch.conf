# SPDX-License-Identifier: LGPL-2.1-or-later

# Kernel parameter configuration—sysctl reads from this directory
L? /etc/sysctl.d/99-sysctl.conf

# pacman configuration file to look up the local database in /usr.
L? /etc/pacman.conf
L? /etc/pacman.d/mirrorlist

# Podman/containers configuration directory
L? /etc/containers
